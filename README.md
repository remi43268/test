# Digits MLP Demo

This project contains a single Jupyter Notebook that demonstrates training a simple neural network on scikit-learn's built-in digits dataset.

## How to run

1. Create or activate a Python environment with `scikit-learn` and `matplotlib` installed.
2. Open the notebook:
   - `digits_mlp_demo.ipynb`
3. Run the cells top to bottom.
